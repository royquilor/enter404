# Images Directory

## Door Fallback Image

Place the fallback poster image here:

- `door-fallback.jpg`

This should be a static image of the door scene (1920x1080 recommended) that will be used:
1. As the poster image for the video element
2. As a fallback when video fails to load
3. As a replacement when `prefers-reduced-motion` is enabled

